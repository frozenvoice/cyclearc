<Window x:Class="CycleArc.UI.ClaudeConnectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="clr-namespace:CycleArc.UI"
        Style="{StaticResource AppWindow}" Width="610" Height="580" MinWidth="470" MinHeight="400"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False" WindowStyle="None"
        AllowsTransparency="True" Background="Transparent" ResizeMode="NoResize" PreviewKeyDown="OnKeyDown">
    <Window.Resources>
        <Style x:Key="GuideText" TargetType="TextBlock">
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="Foreground" Value="{DynamicResource MutedBrush}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="LineHeight" Value="19"/>
            <Setter Property="Margin" Value="0,9,0,0"/>
        </Style>
    </Window.Resources>
    <Border Background="{DynamicResource BgBrush}" BorderBrush="{DynamicResource LineBrush}"
            BorderThickness="1" CornerRadius="14" Padding="22">
        <Grid>
            <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
            <StackPanel MouseLeftButtonDown="OnDrag">
                <TextBlock x:Name="Heading" FontSize="22" FontWeight="SemiBold"/>
                <DockPanel Margin="0,10,0,14">
                    <ui:UsageProviderBadge Provider="Claude" DockPanel.Dock="Left"/>
                    <TextBlock x:Name="ProfileName" Margin="8,0,0,0" FontWeight="SemiBold" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                </DockPanel>
            </StackPanel>
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <StackPanel Margin="0,0,8,0">
                    <TextBlock x:Name="SetupSteps" Style="{StaticResource GuideText}" Margin="0,0,0,14"/>
                    <Border Background="{DynamicResource CardBrush}" BorderBrush="{DynamicResource LineBrush}" BorderThickness="1" CornerRadius="10" Padding="16">
                        <StackPanel>
                            <TextBlock x:Name="ConnectionState" FontWeight="SemiBold" FontSize="16" TextWrapping="Wrap"/>
                            <TextBlock x:Name="AccountIdentity" Style="{StaticResource GuideText}"/>
                            <WrapPanel Margin="0,14,0,0">
                                <Button x:Name="ReauthenticateButton" Style="{StaticResource CreditUseButton}" Click="OnReauthenticate" Margin="0,0,8,8" Visibility="Collapsed"/>
                                <Button x:Name="ConnectExistingButton" Style="{StaticResource CreditUseButton}" Click="OnConnectExisting" Margin="0,0,8,8"/>
                                <Button x:Name="LoginButton" Style="{StaticResource CreditUseButton}" Click="OnLogin" Margin="0,0,0,8"/>
                            </WrapPanel>
                            <Button x:Name="OpenClaudeButton" Style="{StaticResource CreditUseButton}" Click="OnOpenClaude" HorizontalAlignment="Left" Visibility="Collapsed"/>
                        </StackPanel>
                    </Border>
                    <TextBlock x:Name="FreshnessHint" Style="{StaticResource GuideText}"/>
                    <Button x:Name="UsagePageButton" Style="{StaticResource CreditUseButton}" Click="OnUsagePage" HorizontalAlignment="Left" Margin="0,12,0,0"/>
                    <TextBlock x:Name="UsagePageHint" Style="{StaticResource GuideText}"/>
                    <Expander x:Name="AdvancedDetails" Margin="0,16,0,4">
                        <StackPanel Margin="4,10,4,0">
                            <TextBlock x:Name="ExistingStatusLineHint" Style="{StaticResource GuideText}" Margin="0"/>
                            <TextBlock x:Name="ConfigPath" Style="{StaticResource GuideText}"/>
                            <WrapPanel Margin="0,10,0,0">
                                <Button x:Name="DisconnectButton" Style="{StaticResource CreditUseButton}" Click="OnDisconnect" Margin="0,0,8,8" Visibility="Collapsed"/>
                                <Button x:Name="DocsButton" Style="{StaticResource CreditUseButton}" Click="OnDocs" Margin="0,0,0,8"/>
                            </WrapPanel>
                        </StackPanel>
                    </Expander>
                </StackPanel>
            </ScrollViewer>
            <StackPanel Grid.Row="2" Margin="0,14,0,0">
                <ProgressBar x:Name="OperationProgress" Height="3" IsIndeterminate="True" Visibility="Collapsed" Foreground="{DynamicResource AccentBrush}" Background="{DynamicResource GhostBrush}"/>
                <DockPanel Margin="0,10,0,0">
                    <Button x:Name="DoneButton" DockPanel.Dock="Right" Style="{StaticResource CreditUseButton}" Click="OnDone" Margin="12,0,0,0"/>
                    <Button x:Name="CancelButton" DockPanel.Dock="Right" Style="{StaticResource CreditUseButton}" Click="OnCancel" Visibility="Collapsed" Margin="8,0,0,0"/>
                    <TextBlock x:Name="OperationStatus" Style="{StaticResource GuideText}" VerticalAlignment="Center" Margin="0"/>
                </DockPanel>
            </StackPanel>
        </Grid>
    </Border>
</Window>
